# OS
.DS_Store

# Local Claude settings
.claude/settings.local.json

# Design sources (kept local, not deployed)
_archived/

# Original photos (only processed webp output is deployed)
originals/

# Local-only: gallery number picker
gallery-index.html

# Pre-ceremony video source material (raw footage + full export; only
# the encoded copy in wedding/ is deployed)
pre-ceremony/